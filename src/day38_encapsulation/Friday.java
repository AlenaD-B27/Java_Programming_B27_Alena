package day38_encapsulation;

public class Friday { // .java file should be the same as the public class.
}

// I am here, I am still in .java file but not in a class.


class Monday {
    // it's still a class, but it has default access
    // can not be public as the name doesn't match .java file name
    // can not be private => wrong syntax
}